name: yarn_inventory_manager
description: "A virtual yarn stash!"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_echart: ^2.0.0
  cupertino_icons: ^1.0.8
  flutter_colorpicker: ^1.1.0
  shared_preferences: ^2.5.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  test: any

  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/yarn.jpg
  uses-material-design: true


